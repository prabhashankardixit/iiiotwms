spring:
  application:
    name: identity-service

  datasource:
    url: jdbc:postgresql://localhost:5432/iiiotwms
    username: postgres
    password: psd@1185

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
